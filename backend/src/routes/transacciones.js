const router = require('express').Router()

// Placeholder — se implementará en Tarea 3
router.get('/', (req, res) => res.json({ data: [], message: 'Endpoint de transacciones — próximamente' }))

module.exports = router
