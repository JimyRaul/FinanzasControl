const router = require('express').Router()

// Placeholder — se implementará en Tarea 4
router.get('/', (req, res) => res.json({ data: {}, message: 'Endpoint de dashboard — próximamente' }))

module.exports = router
