const router = require('express').Router()

// Placeholder — se implementará en Tarea 2
router.get('/', (req, res) => res.json({ data: [], message: 'Endpoint de categorías — próximamente' }))

module.exports = router
